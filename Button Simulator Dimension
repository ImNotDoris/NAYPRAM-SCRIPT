_G.MainColor = Color3.fromRGB(0, 0, 0)
_G.SecondaryColor = Color3.fromRGB(0, 0, 0)
_G.TertiaryColor = Color3.fromRGB(0,0,0)
_G.ArrowColor = Color3.fromRGB(255, 85, 0)
_G.MainTextColor = Color3.fromRGB(255,255,255)
_G.PointerColor = Color3.fromRGB(255,255,255)
_G.ButtonTextColor = Color3.fromRGB(255,255,255)
_G.SliderColor = Color3.fromRGB(255, 85, 0)
_G.ButtonColor = Color3.fromRGB(255, 85, 0)
_G.ToggleColor = Color3.fromRGB(255, 85, 0)
_G.DraggerCircleColor = Color3.fromRGB(255, 85, 0)
_G.BindColor = Color3.fromRGB(255, 85, 0)
local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/ImNotDoris/NAYPRAM/main/Libary')))()
local All = library:CreateWindow("All")
local AllF = All:CreateFolder("Farm")
AllF:Toggle("AutoFarm",function(bool)
    getgenv().AutoFarm = bool
    local playerhead = game.Players.LocalPlayer.Character.Head
    Spawn(function ()
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        wait(1)
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        wait(1)
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        wait(1)
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        wait(1)
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
        while wait() do
            if not getgenv().AutoFarm then return end
            for i, v in pairs(game:GetService("Workspace").FullButtons.MultiButtons:GetDescendants()) do
                if v.ClassName == "TouchTransmitter" then
                    firetouchinterest(playerhead, v.Parent, 0)
                    firetouchinterest(playerhead, v.Parent, 1)
                end
            end
        end
    end)
end)
